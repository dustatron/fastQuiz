import styled from "styled-components";

const Logo = styled.div`
  display: inline-block;
  font-weight: 600;
  font-size: 1.2rem;

  span:first-of-type {
    font-weight: 200;
  }
`;

export default Logo;
