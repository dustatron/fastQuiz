import React from "react";
import { RowCenter, Card } from "../Styled";

const NoGames = () => {
  return (
    <RowCenter>
      <Card>
        Please <strong>Make</strong> a game first.
      </Card>
    </RowCenter>
  );
};

export default NoGames;
